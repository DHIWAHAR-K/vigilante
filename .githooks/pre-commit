#!/bin/sh
set -eu

repo_root=$(git rev-parse --show-toplevel)
branch=$(git branch --show-current)

if [ "$branch" = "main" ]; then
  echo "commits to main are blocked; use a short-lived codex/<type>-<topic> branch" >&2
  exit 1
fi

"$repo_root/scripts/validate-governance.sh"
